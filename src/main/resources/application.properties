spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway
spring.datasource.username=root
spring.datasource.password=jhvZcTAYPEpfheLHTmYeJiSzAonatNEE

# ==============================
# JPA / HIBERNATE CONFIGURATION
# ==============================

# Show Hibernate SQL queries
logging.level.org.hibernate.SQL=DEBUG

# Format SQL queries
spring.jpa.properties.hibernate.format_sql=true

# Automatically create or update the database schema
spring.jpa.hibernate.ddl-auto=update

# Set Hibernate dialect for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==============================
# SERVER CONFIG (Optional)
# ==============================

# Optional: set server port if needed
# server.port=8080
